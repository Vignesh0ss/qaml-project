# Flask & API
gunicorn>=21.2
flask>=3.0
flask-cors>=4.0
flask-pymongo>=2.3
flask-jwt-extended>=4.6
python-dotenv>=1.0

# Optional: Celery + Redis
celery[redis]>=5.3
redis>=5.0

# Scientific
numpy>=1.24
pandas>=2.0
scikit-learn>=1.4

# Quantum / QUBO (optional)
# dimod>=0.12
# dwave-ocean-sdk>=6.0

# Cheminformatics (for fingerprints)
rdkit>=2023.9.1

# Auth (simple hash for dev; use bcrypt in prod)
bcrypt>=4.0
Flask-Bcrypt>=1.0.1

# Real-time WebSocket support
Flask-SocketIO>=5.3.6
simple-websocket>=1.0.0

# Testing
pytest>=7.0
pytest-cov>=4.0
mongomock>=4.1

