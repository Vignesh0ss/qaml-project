global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'quantum_drug_repurposing_backend'
    static_configs:
      - targets: ['backend:5000']
    metrics_path: '/api/v1/metrics' # Assuming a metrics endpoint will be added
